---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "tableau module encrypted secrets"
    lookup_key: eyaml_lookup_key #
    path: 'secrets.yaml'
    options:
      pkcs7_private_key: /etc/puppetlabs/puppet/eyaml/private_key.pkcs7.pem
      pkcs7_public_key:  /etc/puppetlabs/puppet/eyaml/public_key.pkcs7.pem
